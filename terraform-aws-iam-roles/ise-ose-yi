From your new screenshot, here are the key issues causing the Ivanti Agent installation to fail again:

⸻

❌ Errors Observed:
	1.	Invalid package path:

installer: Error – the package path specified was invalid: “/Users/waade/Downloads/Ivanti.Neurons.Agent.dmg”.

🔍 The command is trying to install a .dmg file directly as a package, but installer -pkg expects a .pkg file inside a mounted .dmg. You need to mount the .dmg first, then point to the .pkg inside it.

	2.	Agent binary not found:

zsh: no such file or directory: /usr/local/com.ivanti.cloud.agent/IvantiAgent/bin/stagentctl

This error confirms the agent wasn’t installed — because the previous step failed.

⸻

✅ Step-by-Step Fix:

🔧 Corrected Manual Installation Commands:

Run these exact commands, step by step:

# Step 1: Mount the DMG file
hdiutil attach ~/Downloads/Ivanti.Neurons.Agent.dmg -mountpoint /tmp/cloudinstall -nobrowse

# Step 2: Run the installer (must be the .pkg inside the mounted image)
sudo /usr/sbin/installer -pkg /tmp/cloudinstall/Ivanti\ Neurons\ Agent.pkg -target /

# Step 3: Detach the DMG
hdiutil detach /tmp/cloudinstall

# Step 4: Register the agent (only after verifying the agent is installed)
sudo /usr/local/com.ivanti.cloud.agent/IvantiAgent/bin/stagentctl register \
  --baseurl https://agentreg.ivanticloud.com \
  --enrollmentkey 03ebe323-1e8e-49e5-bd48-0f15bd53d8ad_Yme7nvknhsd8W8recMf5B39NOdkSPZnsqQLoOeU7Cr8jK9IQBfR34jNpNzAcWQ-D-S58UZSkcmIrFVHVv3cwFSG0RZuVGt3h9CvLxJow3leQNaqRPE0ehsRN7fc_yVh


⸻

✅ Optional: Shell Script Version

If you want a script, you can use this final version (after downloading and customizing the enrollment key if needed):

#!/bin/bash

set -euo pipefail

DMG="$HOME/Downloads/Ivanti.Neurons.Agent.dmg"
MOUNT="/tmp/cloudinstall"
PKG="$MOUNT/Ivanti Neurons Agent.pkg"
STAGENT="/usr/local/com.ivanti.cloud.agent/IvantiAgent/bin/stagentctl"
ENROLL_KEY="03ebe323-1e8e-49e5-bd48-0f15bd53d8ad_Yme7nvknhsd8W8recMf5B39NOdkSPZnsqQLoOeU7Cr8jK9IQBfR34jNpNzAcWQ-D-S58UZSkcmIrFVHVv3cwFSG0RZuVGt3h9CvLxJow3leQNaqRPE0ehsRN7fc_yVh"

echo "[*] Mounting DMG..."
hdiutil attach "$DMG" -mountpoint "$MOUNT" -nobrowse

echo "[*] Installing package..."
sudo installer -pkg "$PKG" -target /

echo "[*] Unmounting DMG..."
hdiutil detach "$MOUNT"

echo "[*] Registering agent..."
sudo "$STAGENT" register --baseurl https://agentreg.ivanticloud.com --enrollmentkey "$ENROLL_KEY"

echo "[✔] Ivanti Agent installed and registered."

Let me know if you want me to generate a downloadable .sh file.
